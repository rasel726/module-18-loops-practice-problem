/**
 * 1. Loop variable
 * 2. condition inside while
 * 3. Loop body
 * 4. Change the loop varible
 */

// if(condition){

// }

    // rostGiven = rostGiven + 1;
    // rostGiven += 1;


var rostGiven = 0;
while(rostGiven < 7) {
    console.log('Rost den, Please !!')
    console.log(rostGiven);
    rostGiven++;
}